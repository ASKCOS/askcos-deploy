apiVersion: v1
kind: Service
metadata:
  name: {{ include "askcos.fullname" . }}
  labels:
    {{- include "askcos.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "askcos.selectorLabels" . | nindent 4 }}
